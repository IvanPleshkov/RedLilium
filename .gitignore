# Rust build artifacts
/target/
**/target/

# Cargo lock for libraries (keep for binaries)
# Cargo.lock  # Uncomment if this becomes a library-only project

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# WASM build output
demos/web/pkg/

# Slang SDK — provisioned locally by scripts/fetch-slang.sh (a real dir), or a
# dev symlink to an existing SDK. No trailing slash so both forms are ignored.
.slang
# KTX-Software CLI — provisioned locally by scripts/fetch-ktx.sh (#120/#122)
.ktx
# Source HDRI for the IBL bake — provisioned by scripts/fetch-hdri.sh (#137)
.hdri

# Environment
.env
.env.local

# Logs
*.log

# mdbook generated output
/book/book/
/book/redlilium-book.html
/redlilium-book.zip
project-assets/
.redlilium/
.claude/settings.local.json
